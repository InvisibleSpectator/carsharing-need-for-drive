import React from "react";
import "./Spinner.scss";

const Spinner = () => {
  return (
    <div className="Spinner">
      <div className="Spinner-Spinner" />
    </div>
  );
};

export default Spinner;
